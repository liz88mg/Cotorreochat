# CotorreoChat

Render-ready Node.js + Express + Socket.IO starter.

## Render
Build Command: `npm install`
Start Command: `npm start`
Environment: Node

The server listens on `process.env.PORT` and `0.0.0.0` and includes `/health`.
